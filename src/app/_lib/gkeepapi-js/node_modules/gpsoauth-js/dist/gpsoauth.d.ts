import { AuthResult, OAuthConfig, TokenExchangeConfig } from './types';
/**
 * Modern JavaScript/TypeScript implementation of Google Play Services OAuth (gpsoauth)
 * Compatible with React Native and Node.js environments
 */
export declare class GPSOAuth {
    /**
     * Exchange web oauth token for master token
     */
    static exchangeToken(email: string, token: string, config?: TokenExchangeConfig): Promise<AuthResult>;
    /**
     * Perform OAuth to a specific Google service using master token
     */
    static performOAuth(email: string, masterToken: string, config?: OAuthConfig): Promise<AuthResult>;
    /**
     * Make HTTP request to Google's auth endpoint
     */
    private static makeAuthRequest;
    /**
     * Parse Google's authentication response
     */
    private static parseAuthResponse;
    /**
     * Generate a mock Android device ID
     */
    private static generateAndroidId;
}
//# sourceMappingURL=gpsoauth.d.ts.map